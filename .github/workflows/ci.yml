name: CI
on: [push]
jobs:
  lighthouseci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      # Update Node.js to version 18.17.0 or higher
      - uses: actions/setup-node@v1
        with:
          node-version: '18.17' # Specify Node.js version 18.17.0 or higher
          cache: 'pnpm'         # Enable caching for pnpm

      # Install pnpm
      - run: npm install -g pnpm

      # Install project dependencies with pnpm
      - run: pnpm install

      # Install Lighthouse CI with npm
      - run: npm install -g @lhci/cli@0.6.x

      # Build and run Lighthouse CI
      - run: pnpm run build
      - run: lhci autorun
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
